# Components

可复用的 UI 组件目录

## 用途
- 存放通用 UI 组件（按钮、卡片、表单等）
- 组件应该是无状态或轻状态的
- 每个组件建议独立文件夹，包含 .tsx 和样式文件

## 示例结构
```
components/
├── Button/
│   ├── Button.tsx
│   └── index.ts
└── Card/
    ├── Card.tsx
    └── index.ts
```
