# Hooks

自定义 React Hooks 目录

## 用途
- 可复用的状态逻辑
- 副作用封装
- 自定义数据获取 hooks

## 命名规范
- 所有 hooks 必须以 `use` 开头
- 例如：`useAuth.ts`, `usePetData.ts`

## 示例
- `useAuth.ts` - 用户认证相关（待开发）
- `useLocalStorage.ts` - 本地存储封装（待开发）
